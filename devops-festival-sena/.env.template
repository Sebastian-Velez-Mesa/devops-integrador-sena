# ── Configuración de Entorno para el API Backend ──────────────────────
# Entorno de ejecución: production o development
FLASK_ENV=production

# Puerto expuesto por el contenedor del backend
PORT=5000

# Directorio interno del contenedor donde se persistirán los logs
LOGS_DIR=/app/logs
